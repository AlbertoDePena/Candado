<UserControl x:Class="Candado.Desktop.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid FocusManager.FocusedElement="{Binding ElementName=Login}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="180" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="120" />
            <RowDefinition Height="40" />
            <RowDefinition Height="40" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <Image Grid.Row="1" Grid.Column="1" Source="../Images/password.png" Width="90" Height="90"/>

        <DockPanel Grid.Row="2" Grid.Column="1" ToolTip="Password">
            <Image Source="../Images/password.png" />
            <PasswordBox x:Name="PasswordBoxControl"/>
        </DockPanel>

        <Button x:Name="Login" Grid.Row="3" Grid.Column="1" Content="Login" />
    </Grid>
</UserControl>
